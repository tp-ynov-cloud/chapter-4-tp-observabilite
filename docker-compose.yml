services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - .env

  db:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro

volumes:
  db-data:
